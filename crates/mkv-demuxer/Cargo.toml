[package]
name = "mkv-demuxer"
version = "0.1.0"
edition = "2021"
description = "Small Matroska probe compiled to WebAssembly for MX Player Pro"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
